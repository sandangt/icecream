name: "Consul service script"

on:
  workflow_dispatch:
    inputs:
      ENV:
        type: choice
        options: [DEV, STAG, PROD]
        required: true
        default: DEV

run-name: "[${{ inputs.ENV }}](🚀): consul-service"

jobs:
  build:
    name: Build Icecream Consul Service (${{ inputs.ENV }})
    uses: ./.github/workflows/build-spring-boot-service.yaml
    with:
      root-path: icecream-system
      module: consul
      image-name: icecream-consul-service
      ENV: ${{ inputs.ENV }}
    secrets: inherit
